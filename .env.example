# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# LobeHub Builtin Blocker 配置文件
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 状态更新 (2025-07):
#   官方 canary 已支持 per-agent 禁用内置工具，本补丁项目仅作为过渡/全局兜底。
#   推荐优先使用官方方案：在助手设置里禁用下面列出的污染工具，并保留 memory 开启。
#   只有 MCP session 官方修复尚未合并时，才需要启用 ENABLE_MCPFIX。

# ── 补丁开关 ──────────────────────────────────────────────
# 是否启用 MCP session 修复 (P1-P5, 5个补丁点)
# 修复 MCP 工具调用后的 session 缓存问题
# 官方修复分支: fix/mcp-session-retry (withSessionRetry)
# 待合并到 canary/main 并在 Docker 镜像可用后，可设为 false
ENABLE_MCPFIX=true

# 是否启用内置工具黑名单 (P1-P7, 7个补丁点)
# 如果官方 per-agent 禁用已可用，建议优先用官方方案，本开关可设为 false
ENABLE_ANTIPOLLUTE=true

# ── 推荐黑名单工具列表 ────────────────────────────────────
# Comma-separated identifiers to disable / 逗号分隔，禁用以下内置工具:
#
# 推荐关闭 (会注入污染性 systemRole):
#   - lobe-web-browsing: 强制联网搜索 / 强制脚注
#       官方替代方案: DEFAULT_AGENT_CONFIG=chatConfig.searchMode=off
#       (仅作为默认值，不覆盖已显式开启搜索的助理)
#   - lobe-task: 任务管理 / 小问题也建清单
#   - lobe-delivery-checker: 交付验收清单
#   - agent-signal-review/reflection/feedback-intent/skill-management: 自我反思类
#   - lobe-self-iteration: 自我迭代
#   - lobehub: LobeHub 平台身份技能
#
# 不建议关闭:
#   - memory: 自动记忆是核心体验，保留开启
#   - knowledge-base / cloud-sandbox: 仅在特定条件下启用，污染低
DISABLED_BUILTIN_TOOLS=lobe-web-browsing,lobe-task,lobe-delivery-checker,agent-signal-review,agent-signal-reflection,agent-signal-feedback-intent,agent-signal-skill-management,lobe-self-iteration,lobehub
